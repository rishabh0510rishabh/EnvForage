apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "envforage.fullname" . }}-config
  labels:
    app: {{ include "envforage.fullname" . }}
data:
  {{- range $key, $val := .Values.env }}
  {{ $key }}: {{ $val | quote }}
  {{- end }}
