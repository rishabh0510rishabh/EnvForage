apiVersion: v1
kind: Service
metadata:
  name: {{ include "envforage.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "envforage.fullname" . }}-backend
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8000
